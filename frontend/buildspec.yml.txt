version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing NPM dependencies...
      - npm ci  # dùng ci thay vì install nếu bạn dùng CI/CD để đảm bảo sạch và nhanh hơn

  build:
    commands:
      - echo Building the React application...
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: build
